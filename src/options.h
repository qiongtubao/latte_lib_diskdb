#ifndef __LATTE_OPTIONS_H
#define __LATTE_OPTIONS_H
typedef struct _DiskDbOptions {
    bool create_if_missing;
    bool error_if_exists;
} DiskDbOptions;

typedef struct Options {

} Options;

#endif